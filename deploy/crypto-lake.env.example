# Environment variables for Crypto Lake service
# Copy this file to /etc/default/crypto-lake and customize

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=WARNING

# GCS bucket for data backup
GCS_BUCKET_NAME=crypto-lake-data

# GCP project ID
GCP_PROJECT_ID=serious-conduit-476419-q7

# Database connection URL (optional, defaults to DuckDB in-memory)
# DATABASE_URL=duckdb:///./crypto.duckdb
# DATABASE_URL=postgresql://user:pass@localhost:5432/crypto_lake

# Google Cloud SQL connection (optional)
# CLOUD_SQL_CONNECTION_NAME=project:region:instance
